<?php
include_once 'connection.php';
?>

<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo $WEB_TITLE; ?></title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
			font-family: 'Arial', sans-serif;
			background: url('images/testimonial_backgroung.jpg') no-repeat center center fixed;
			background-size: cover;
			display: flex;
			justify-content: center;
			align-items: center;
			height: 100vh;
			margin: 0;
			color: #4a4a4a;
			position: relative; /* Make it relative for overlay positioning */
		}

		/* Transparent overlay without blurring the background */
		.overlay {
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background: rgba(0, 0, 0, 0.5); /* 30% Black Transparency */
			z-index: 0; /* Keeps it behind the form */
		}

		/* Form container with transparency */
		.container {
			background-color: rgba(255, 255, 255, 0.85); /* Slight transparency for glass effect */
			width: 90%;
			max-width: 700px;
			padding: 40px;
			border-radius: 25px;
			box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
			text-align: center;
			position: relative;
			z-index: 1; /* Keeps the form above the overlay */
		}

        h3 {
            font-size: 2em;
            color: #D55A8F;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .star-rating {
            display: flex;
            justify-content: center;
            direction: rtl;
            margin-bottom: 20px;
        }

        .star-rating input {
            display: none;
        }

        .star-rating label {
            font-size: 3em;
            color: #ddd;
            cursor: pointer;
            transition: color 0.3s ease, transform 0.3s ease-in-out;
        }

        .star-rating label:hover,
        .star-rating input:checked~label {
            color: #FF1493;
            transform: scale(1.2);
        }

        textarea {
            width: 100%;
            padding: 15px;
            border-radius: 10px;
            border: 2px solid #ddd;
            font-size: 1.1em;
            background-color: #fafafa;
        }

        textarea:focus {
            border-color: #FF1493;
            box-shadow: 0 0 10px rgba(255, 20, 147, 0.3);
            outline: none;
        }

        button {
            width: 100%;
            padding: 15px;
            background-color: rgb(100, 57, 80);
            color: white;
            border: none;
            border-radius: 15px;
            font-size: 1.3em;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #D55A8F;
        }

        .feedback-message {
            margin-top: 20px;
            text-align: center;
            color: #27ae60;
            font-size: 1.2em;
            font-weight: bold;
        }

        .error-message {
            margin-top: 20px;
            text-align: center;
            color: #e74c3c;
            font-size: 1.2em;
            font-weight: bold;
        }
		
		/* Close Button Styling */
		.close-btn {
			position: absolute;
			top: 15px;
			right: 20px;
			font-size: 28px;
			color: #555;
			text-decoration: none;
			font-weight: bold;
			transition: color 0.3s ease;
		}

		.close-btn:hover {
			color: #D55A8F;
		}
    </style>
</head>

<body>

    <div class="overlay"></div> 

    <div class="container">
		<!-- Close Button -->
		<a href="index.php" class="close-btn">&times;</a>
        <h3>Share Your Matrimonial Experience</h3>

        <?php
        if (isset($_POST['submitFeedback'])) {
            $rating = $_POST['rating'];
            $testimonial_description = $_POST['feedback'];
            $member_id = $_SESSION['member_id'];
            $testimonial_date = date('Y-m-d');
            $testimonial_status = '1';

            $query = "INSERT INTO testimonial_tbl (member_id, testimonial_date, rating, testimonial_description, testimonial_status) 
                      VALUES ('$member_id', '$testimonial_date', '$rating', '$testimonial_description', '$testimonial_status')";

            if (mysqli_query($conn, $query)) {
                echo "<div class='feedback-message'>Thank you for your valuable feedback! 💖</div>";
                echo "<script>setTimeout(function(){ window.location.href = 'index.php'; }, 2000);</script>";
            } else {
                echo "<div class='error-message'>Error submitting feedback. Please try again.</div>";
            }
        }
        ?>

        <form id="feedbackForm" method="POST" action="">
            <div class="form-group">
                <label for="rating">How would you rate your experience with us?</label>
                <div class="star-rating">
                    <input type="radio" id="star5" name="rating" value="5" required>
                    <label for="star5">★</label>
                    <input type="radio" id="star4" name="rating" value="4">
                    <label for="star4">★</label>
                    <input type="radio" id="star3" name="rating" value="3">
                    <label for="star3">★</label>
                    <input type="radio" id="star2" name="rating" value="2">
                    <label for="star2">★</label>
                    <input type="radio" id="star1" name="rating" value="1">
                    <label for="star1">★</label>
                </div>
            </div>

            <div class="form-group">
                <label for="feedback">We'd love to hear your thoughts:</label>
                <textarea id="feedback" name="feedback" rows="4" placeholder="Write your feedback here..." required></textarea>
            </div>

            <button type="submit" name="submitFeedback">Submit Feedback</button>
        </form>
    </div>

</body>

</html>
